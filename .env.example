# Linear — https://linear.app/settings/api
LINEAR_API_KEY=
LINEAR_TEAM_ID=
LINEAR_PROJECT_ID=

# GitHub — repo where PRs are opened (owner/name)
GITHUB_TOKEN=
GITHUB_REPO=

# Optional: Cursor SDK coding agent
CURSOR_API_KEY=

# Stretch: refuse merge until a human runs `npm run loop:approve`
REQUIRE_HUMAN_MERGE=0
